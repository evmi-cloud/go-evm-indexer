{
    "database": {
        "type": "POSTGRES",
        "config": {
            "dsn": "host=localhost user=evmi password=secret dbname=evmi port=5432 sslmode=disable"
        }
    },
    "metrics": {
        "enabled": true,
        "path": "/metrics",
        "port": 9090
    },
    "plugins": [
        {
            "name": "erc20-balances",
            "description": "Track ERC-20 balances",
            "gitUrl": "https://github.com/your-org/your-plugin.git",
            "relativePath": "cmd/plugin"
        }
    ]
}
